---
layout: null
sitemap:
  exclude: 'yes'
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <!-- Homepage -->
  <url>
    <loc>{{ site.url }}/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>

  <!-- Pages -->
  {% for page in site.pages %}
    {% unless page.sitemap.exclude == "yes" or page.name contains ".xml" or page.name contains ".txt" or page.name contains ".json" %}
      {% if page.layout != nil and page.layout != 'feed' %}
        <url>
          <loc>{{ site.url }}{{ page.url | remove: 'index.html' }}</loc>
          {% if page.date %}
            <lastmod>{{ page.date | date_to_xmlschema }}</lastmod>
          {% else %}
            <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
          {% endif %}
          {% if page.sitemap.changefreq %}
            <changefreq>{{ page.sitemap.changefreq }}</changefreq>
          {% else %}
            <changefreq>monthly</changefreq>
          {% endif %}
          {% if page.sitemap.priority %}
            <priority>{{ page.sitemap.priority }}</priority>
          {% else %}
            <priority>0.8</priority>
          {% endif %}
        </url>
      {% endif %}
    {% endunless %}
  {% endfor %}

  <!-- Posts -->
  {% for post in site.posts %}
    {% unless post.sitemap.exclude == "yes" %}
      <url>
        <loc>{{ site.url }}{{ post.url }}</loc>
        <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
        {% if post.sitemap.changefreq %}
          <changefreq>{{ post.sitemap.changefreq }}</changefreq>
        {% else %}
          <changefreq>monthly</changefreq>
        {% endif %}
        {% if post.sitemap.priority %}
          <priority>{{ post.sitemap.priority }}</priority>
        {% else %}
          <priority>0.9</priority>
        {% endif %}
      </url>
    {% endunless %}
  {% endfor %}

  <!-- Collections (if any) -->
  {% for collection in site.collections %}
    {% unless collection.output == false or collection.label == "posts" %}
      {% for document in collection.docs %}
        {% unless document.sitemap.exclude == "yes" %}
          <url>
            <loc>{{ site.url }}{{ document.url }}</loc>
            {% if document.date %}
              <lastmod>{{ document.date | date_to_xmlschema }}</lastmod>
            {% else %}
              <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
            {% endif %}
            {% if document.sitemap.changefreq %}
              <changefreq>{{ document.sitemap.changefreq }}</changefreq>
            {% else %}
              <changefreq>monthly</changefreq>
            {% endif %}
            {% if document.sitemap.priority %}
              <priority>{{ document.sitemap.priority }}</priority>
            {% else %}
              <priority>0.7</priority>
            {% endif %}
          </url>
        {% endunless %}
      {% endfor %}
    {% endunless %}
  {% endfor %}

  <!-- Category and Tag Pages -->
  {% for category in site.categories %}
    <url>
      <loc>{{ site.url }}/categories/{{ category[0] | slugify }}/</loc>
      <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
      <changefreq>weekly</changefreq>
      <priority>0.6</priority>
    </url>
  {% endfor %}

  {% for tag in site.tags %}
    <url>
      <loc>{{ site.url }}/tags/{{ tag[0] | slugify }}/</loc>
      <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
      <changefreq>weekly</changefreq>
      <priority>0.5</priority>
    </url>
  {% endfor %}
</urlset>